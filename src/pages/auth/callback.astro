---
import { supabase } from '../../lib/supabase';

const code = Astro.url.searchParams.get('code');

if (code) {
  await supabase.auth.exchangeCodeForSession(code);
}

return Astro.redirect('/celebration');
---
